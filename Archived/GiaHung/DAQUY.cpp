#include <bits/stdc++.h>

using namespace std;
string a;
string b;
int main()
{
    getline(cin,a);
    getline(cin,b);
    for(int i=2;i<a.size();i++)
    {
        if(a[i]<='9' && a[i]>='0' && a[i+4])
    }
    return 0;
}
