#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("ontap51.inp","r",stdin);
    freopen("ontap51.out","w",stdout);
    int n,so[1000],maxn,minn,o;
    cin>>n>>so[1];
    ma=so[1];
    for (int i=2;i<=n;i++)
      {
          cin>>so[i];
      }
      sort(so+1,so+n+1)
    cout<<maxn+minn;
}
