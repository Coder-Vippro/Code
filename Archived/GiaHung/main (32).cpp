#include <bits/stdc++.h>

using namespace std;

bool ktnt(int n)
{
    if(sqrt(n)*sqrt(n)==n)
        return true;
    else return false;
    return n;
}

int main()
{
    int n;
    cin>>n;
    int s=0;
    for(int i=1;i<=n;i++)
    {
        if(ktnt(i)==true)

    }
    cout<<s;
    return 0;
}
