#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("magic.inp","r",stdin);
  freopen("magic.out","w",stdout);
  long long a,b,t=0,sd,sc,s=0;
  cin>>a;
  for(int i=1;i<=a;i++) {cin>>b[i];t=t+b[i]}
  t=t-b[i];
  sd=b[i];sc=b[i-1];
  for(long long k=1;k<=a-1;k++);
  {
      s=s+b[k];
      if((a-1)%k==0&&t%ss==0&&b[k]sc&&b[k+1]==sd)
      {
          cout<<k
      }
  }

    return 0;
}
