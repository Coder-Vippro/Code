#include <bits/stdc++.h>

using namespace std;

int main()
{
    //freopen("CAT.INP","r",stdin);
    //freopen("CAT.OUT","w",stdout);
    float a,b,c,d,e,a1,b1,c1,d1,e1;
    cin>>a>>b>>c;
    abs(a)=a1;
    abs(b)=b1;
    abs(c)=c1;
    c1- a1=d;
    c1- b1=e;
    abs(d)=d1;
    abs(e)=e1;
    if d1 < e1 cout<<"Cat_A";
    else if d1 > e1 cout<<"Cat_B";
    else cout<<"The mouse has escaped";
    return 0;
}
