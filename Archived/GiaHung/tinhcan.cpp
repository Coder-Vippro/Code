#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("tinhcan.inp","r",stdin);
    freopen("tinhcan.out","w",stdout);
    int a;
    cin>>a;
    cout<<fixed<<setprecision(0)<<a+int(sqrt(a));
    return 0;
}
