#include <bits/stdc++.h>
using namespace std;
int n;
int a[1000005];
string s;
long long 
int main()
{
    cin>>s;
    for(int i=0;i<n;i++)
    {
        cin>>s;
        if(s[0]=='H' || s[0]=='H' || s[0]=='H' || s[0]=='H' || s[0]=='H')
        a[s[0]]++;
    }

}